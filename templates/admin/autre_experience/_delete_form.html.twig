<form method="post" action="{{ path('autre_experience_delete', {'id': autre_experience.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autre_experience.id) }}">
    <button class="btn">Delete</button>
</form>
