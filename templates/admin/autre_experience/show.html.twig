{% extends 'base.html.twig' %}

{% block title %}AutreExperience{% endblock %}

{% block body %}
    <h1>AutreExperience</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ autre_experience.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ autre_experience.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('autre_experience_index') }}">back to list</a>

    <a href="{{ path('autre_experience_edit', {'id': autre_experience.id}) }}">edit</a>

    {{ include('autre_experience/_delete_form.html.twig') }}
{% endblock %}
