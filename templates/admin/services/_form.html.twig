{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
    <div class="row col-md-12">
        <div class="form-group col-md-4">
            {{ form_label(form.name, 'Intitule du service') }}
            {{ form_widget(form.name, {'attr': {'class': 'name form-control', 'placeholder': 'Intitule du service'}}) }}
        </div>
        <div class="form-group col-md-4">
            {{ form_label(form.quantity_max, 'Quantite Max') }}
            {{ form_widget(form.quantity_max, {'attr': {'class': 'name form-control', 'type': 'number', 'placeholder': 'Quantite max'}}) }}
        </div>
    </div>
    <div class="row col-md-12">
        <div class="form-group col-md-4">
            {{ form_label(form.price_initial, 'Prix initial') }}
            {{ form_widget(form.price_initial, {'attr': {'class': 'price_initial form-control', 'placeholder': 'Prix initial'}}) }}
        </div>
        <div class="form-group col-md-4">
            {{ form_label(form.price_special, 'Prix special') }}
            {{ form_widget(form.price_special, {'attr': {'class': 'price_special form-control', 'placeholder': 'Prix special'}}) }}
        </div>
    </div>
    <div class="row col-md-12">
        <div class="form-group col-md-4">
            {{ form_label(form.duree, 'Duree') }}
            {{ form_widget(form.duree, {'attr': {'class': 'duree form-control', 'placeholder': 'Duree'}}) }}
        </div>
    </div>
    <div class="row col-md-12">
        <div class="form-group col-md-8">
            {{ form_label(form.description) }}
            {{ form_widget(form.description, {'attr': {'class': 'description form-control'}}) }}
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-2">
            <a class="btn btn-default" href="{{ path('adminPath.services') }}"><i class="fa fa-arrow-circle-left"></i> Retour a la liste</a>
        </div>
        <div class="col-md-2">
            <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        </div>
        {# {% if app.request.uri|slice(-4) == 'edit' %}
            <div class="col-md-2">
                {{ include('services/_delete_form.html.twig') }}
            </div>
        {% endif %} #}
    </div>
{{ form_end(form) }}
