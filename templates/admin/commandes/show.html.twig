<!DOCTYPE html>

<title>Commandes</title>

{% block body %}
    <h1>Commandes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Code_commande</th>
                <td>{{ commande.codeCommande }}</td>
            </tr>
            <tr>
                <th>Com_createdAt</th>
                <td>{{ commande.comCreatedAt ? commande.comCreatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ commande.duree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commandes_index') }}">back to list</a>

    <a href="{{ path('commandes_edit', {'id': commande.id}) }}">edit</a>

    {{ include('commandes/_delete_form.html.twig') }}
{% endblock %}
