<form method="post" action="{{ path('adminPath.employee_delete', {'id': employee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> Suppression</button>
</form>
